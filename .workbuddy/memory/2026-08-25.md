- 排查 BiliNote 生成笔记过程中的产物存放地址：确认最终 Markdown 默认落在 `backend/note_results/`，任务 JSON/转写/状态缓存落在 `backend/json_results/`，RAG 向量索引落在 `vector_db/`，下载的音视频缓存默认落在 `backend/data/data/`，视频理解中间帧/拼图落在 `backend/data/output_frames/` 与 `backend/data/grid_output/`，截图和上传目录会因启动目录不同落在根目录或 `backend/` 下的 `static/screenshots`、`uploads`。
- 已按需求调整任务历史：生成历史拆成“批次任务 / 单个任务”两个页签；批次展开后可直接查看当前筛选下的全部子任务（不再分页）；删除批次改为“移出批次视图”，仅清除任务的 `batch_id/batch_name` 归属，不删除已生成的笔记内容或结果文件。前端构建与后端语法校验均已通过。
- 已通过项目启动脚本完成一次健康检查，确认前端 `http://localhost:3015/`、后端 `http://localhost:8483/` 可就绪；当前脚本优先使用 `backend/.venv313/bin/python` 作为后端解释器。
- 在当前 WorkBuddy 非交互后台执行场景下，`启动BiliNote.command` 结束后其子进程不会持续保活；要稳定启动需分别后台运行后端 `backend/.venv313/bin/python backend/main.py`（带 `FFMPEG_BIN_PATH=/Users/jiuyue/.local/bin`、`HF_ENDPOINT=https://hf-mirror.com`、`WHISPER_MODEL_SIZE=tiny`）和前端 `cd BillNote_frontend && pnpm dev --host 0.0.0.0`。
- 已修复历史页签区的纵向滚动：`History.tsx` 中页签内容容器改为 `flex-1 + overflow-y-auto`，批次任务展开后可在页签内滚动浏览；同时将页签顺序调整为“单个任务”在前、“批次任务”在后，并把默认页签切换为“单个任务”。前端构建已再次通过。
- 已按新要求补回分页能力：批次任务卡片列表增加顶层分页（当前每页 5 个批次），批次展开后的子任务列表重新启用分页（每页沿用 `HISTORY_PAGE_SIZE`），筛选切换时会自动回到子任务第 1 页。前端构建已通过。
- 修复 `/api/transcriber_models_status` 在 macOS 未安装可选依赖 `mlx_whisper` 时返回 `500000`：状态接口对 MLX 模块导入增加 `ImportError` 隔离，缺包时返回 `mlx_whisper: []`、`mlx_available: false`，不影响 fast-whisper 状态查询。
- 已为 BiliNote 创建 macOS 持久化启动配置：`~/Library/LaunchAgents/com.jiuyue.bilinote.backend.plist` 和 `com.jiuyue.bilinote.frontend.plist`，均启用 `RunAtLoad` 与 `KeepAlive`。当前 WorkBuddy 非交互会话向 `launchd` 注册时返回 `Bootstrap failed: 5: Input/output error`，因此本次未确认已加载；配置文件已保留，可在普通 Terminal 手动执行 `launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.jiuyue.bilinote.backend.plist` 及前端对应命令完成立即加载。
- 已排查并修复“历史数据为空”：根因是 `backend/app/utils/output_paths.py` 的默认输出目录 `backend/json_results`、`backend/note_results` 使用相对路径，后端以 `backend/` 为工作目录运行时会错误解析成 `backend/backend/json_results`，导致 `/api/history` 与 `/api/task_status` 读不到真实结果文件，把已有任务回退成 FAILED 并从历史列表过滤掉。现已将输出目录统一改为基于仓库根目录解析，并补充 `backend/tests/test_output_paths.py` 回归测试；使用项目虚拟环境跑通 `test_output_paths.py` 与 `test_init_db_migration.py`。重启后端后，`/api/history?limit=5&offset=0` 已恢复返回历史记录，`/api/history/42fd60de-cf5b-46e9-8397-e0bdb992d43a` 已恢复为 `SUCCESS` 且能读出完整 `result`。
- 已继续修复历史分页与前端校验：`backend/app/routers/note.py` 的 `/history` 改为按“过滤后条目数达到 offset+limit”为止继续取数，避免旧实现先对数据库分页、再对过滤结果二次切片导致跳页/空页；新增 `backend/tests/test_history_api.py` 覆盖过滤后分页场景，配合原有测试共 6 项通过。浏览器实测前端 `http://localhost:3015/` 已恢复展示“单个任务(4)/批次任务(1)”历史列表，详情面板可正常展示；同时发现 `BillNote_frontend/src/pages/HomePage/components/NoteHistory.tsx` 在仅 1 页数据时“下一页”按钮仍可点击，已补充禁用条件并触发前端构建验证。
- 已将 `启动BiliNote.command` 改为“优先写入并尝试加载 LaunchAgent，失败时回退当前会话直接启动”的持久化模式；新增 `bilinote-backend-launch.sh` 与 `bilinote-frontend-launch.sh` 作为 LaunchAgent 包装脚本，并让生成的 `~/Library/LaunchAgents/com.jiuyue.bilinote.{backend,frontend}.plist` 指向这两个脚本。当前 WorkBuddy 会话仍无法直接完成 `launchctl bootstrap`（`backend_agent_loaded=113` / `frontend_agent_loaded=113`），但脚本已能自动给出 Terminal 手动加载命令，同时继续把后端成功拉起到 `http://localhost:8483/`，前端现有监听保持在 `http://localhost:3015/`。
- 完成产物路径彻底统一（消除根目录/backend 目录混用）：在 `app/utils/path_helper.py` 新增 `APP_ROOT`/`WORKSPACE_ROOT` 与 `resolve_app_path()`/`resolve_workspace_path()`，一切相对路径锚定到 main.py 所在目录（本地=backend/，Docker=/app）。改造范围：output_paths（兼容 .env 旧写法 `backend/note_results` 自动剥前缀）、main.py 的 static/uploads/out_dir、note.py 的 IMAGE_OUTPUT_DIR、vector_store（未设 env 时优先沿用根目录已有 vector_db）、4 个 config 管理器 json、routers/note 的 UPLOAD_DIR、video_helper 封面目录、downloaders/base 的 DATA_DIR 兜底、local_downloader 的 /uploads 解析。已删除垃圾目录 backend/backend/、根目录 static/ 与 uploads/。test_output_paths 测试同步改写为 APP_ROOT 口径；修复 test_douyin_downloader 用 /tmp 残留缓存导致不可重复执行的问题（改用 TemporaryDirectory）。全量测试 108 passed（仅排除需真实联网的 bilibili_dm_patch 一例）。
- 后端"自动关闭"根因确认：WorkBuddy 会话结束后其子进程（含 nohup 派生）会被整体回收，后端随会话死亡；另会话内有 HTTP_PROXY 环境变量，之前 curl 看到的 502 是代理返回的假象，检查服务健康务必加 `--noproxy '*'`。
- 持久化方案已就位：`启动BiliNote.command` 生成的两个 plist 已改为 `KeepAlive={SuccessfulExit:false}`（配合包装脚本的端口占用即 exit 0，避免重启死循环）；新增 `注册BiliNote持久服务.command`，双击即可在 Terminal 里完成 `launchctl bootout+bootstrap` 注册。受 macOS 自动化权限限制（osascript -10004），WorkBuddy 会话内无法代为执行注册；plist 在 `~/Library/LaunchAgents` 且带 RunAtLoad，下次登录也会自动生效。

## 修复任务列表失败任务消失问题（18:30）
- 现象：任务卡在"转录中"，随后从列表消失；失败任务全部不显示。
- 根因：`backend/app/routers/note.py` 的 `/api/history` 把「无结果文件且状态 FAILED」的任务过滤掉了，而前端 `refreshHistoryList` 以后端返回为唯一事实来源 → 失败卡片被整体移除。旧测试 `test_history_api.py` 把该行为固化了。
- 修复：移除 history 中对 FAILED 的过滤（失败任务带 message 返回）；重写测试（新增 keeps_failed 用例，分页过滤用例改用 include_pending=false 验证）。
- 实际失败原因：LLM 供应商返回 403「套餐请求次数已用完」（22 条历史失败任务均此原因），并非转写本身问题。
- 后端由持久服务自动拉起（kill 后自启 PID 80591），无需手动重启；验证 /api/history 已含 22 条 FAILED。

## 后端拉起方式经验（18:45 补充）
- WorkBuddy 环境里 `nohup + & + disown` 分离启动的后端会被会话回收杀掉；`launchctl load/bootstrap` 在此环境报 I/O error（用户终端里 LaunchAgent 本身是好的）。
- 可靠方式：Bash 工具 `run_in_background: true` 跑 `./bilinote-backend-launch.sh`（脚本自带端口检查、env、unset 代理），启动约 10-20 秒（转录初始化那步可能慢几秒）。
- 用户此前手动删过任务：6d4b0fc4、648ed1d1 均删除成功。

## 前端删除后卡片仍在排查（18:55）
- fc247651 服务端三处全干净：db 0 行、history 响应 grep 0 次、batch_status 197 条不含它；JSON 严格解析通过。
- 前端代理默认指向 127.0.0.1:8483（vite.config.ts VITE_API_BASE_URL），数据源唯一。

## 三项界面改动（22:15）
- 历史搜索框旁新增「按标题重命名」按钮：后端新增 `POST /api/fix_note_titles`（`fetch_online` 参数控制本地缺失标题时是否用 yt-dlp 在线补取，不下载视频），判定口径 = DB title 为空/等于 video_id/等于 source_url；优先取 note JSON 的 audio_meta.title，同步 update_task_title + 把 `{task_id}_markdown.md` 改名为 `{标题}.md`。前端 `History.tsx` 加按钮调接口后 `refreshHistoryList()`。
- NoteForm：单条时不再显示「单条模式」徽标（仅批量 N 条时显示）；单个视频链接输入框从平台选择同一行移到下一行独立展示，平台选择框加宽到 w-40/w-48。
- 冒烟结果：202 条任务中 54 条未按标题命名，52 条已通过接口本地修复（含 2 条需在线获取的抖音任务待按钮在线补取）；tsc 对三个改动文件无新增错误（项目存量错误在 DownloaderForm/modelForm 等，与本次无关）。
- 注意：API 前缀是 `/api`（`app/__init__.py` include_router prefix="/api"），不是 `/api/note`。

## 标题重命名不生效修复 + 批量输入禁用逻辑（22:35）
- 用户反馈点按钮后单个任务卡片名没变。根因：历史卡片显示名优先取笔记 JSON 的 `audio_meta.title`（`taskStore.createHistoryTask` 合并逻辑），第一版接口只更新 DB title 没回写 JSON；且 3 条抖音任务通用 yt-dlp 补取报 "Fresh cookies"。
- 修复：`fix_note_titles` 现在同时回写 DB title、笔记 JSON audio_meta.title、markdown 文件名；判定改为「DB 或 JSON 任一是占位名即需修复」；候选标题优先级 本地 JSON > DB > 在线补取；抖音在线补取复用 `DouyinDownloader._extract_info_with_ytdlp`（自动带设置里的 Cookie）。failed 列表带 reason，前端 toast 区分 Cookie 失效提示。
- 全量重跑结果：203 条任务全部有正确标题，3 条抖音在线补取成功，0 失败。tsc 对比基线（git stash 法）：改动前后这 3 个文件都是 31 个存量错误，本次零新增。注意 tsc 输出用 head 截断会漏看后面文件的错误。
- 批量视频链接 Textarea 加上与单个链接输入框一致的禁用逻辑 `disabled={!!editing && batchCount === 0}`（查看/编辑已有单任务时不可输入）。
- 后端启动再次确认：必须 Bash run_in_background:true 直接跑 main.py，普通命令里 nohup+& 会被回收。

## MLX Whisper「模型已下载但不被识别」排查（19:50）
- 现象：`backend/models/mlx-whisper/mlx-community/whisper-tiny-mlx/` 下 config.json、weights.npz 齐全，但前端不认。
- 根因：`.venv313` 里**根本没装 `mlx_whisper` 包**（`ModuleNotFoundError`），`transcriber_provider.py` 顶层 import 失败 → `MLX_WHISPER_AVAILABLE=False`；启动日志 13:59:40 有对应 WARNING。模型文件在不在都无所谓，判定入口是包导入。
- 次要问题：`requirements.txt` 完全没有 mlx 相关条目，`pip install -r requirements.txt` 永远装不上它（可选依赖未声明）。
- 注意：mlx-whisper 直接依赖 numba/torch/scipy/mlx 等，torch 是大头，网络慢时 pip 安装要 20-40 分钟；看到的 `llvmlite-...whl` 是 numba 的传递依赖且为预编译 wheel，不是源码编译，别误判为构建卡死。判活方法：看 `/var/folders/.../T/pip-unpack-*` 最近 60s 是否有新文件。
- 装完必须重启后端，可用性标志只在进程 import 时求值一次。
- **已解决（22:22）**：上一轮后台 pip 装到一半被会话回收（task 消失）；改用清华镜像 `-i https://pypi.tuna.tsinghua.edu.cn/simple` 重装，4 分 23 秒装完（mlx 0.32.2 + mlx-metal + torch 2.13 + numba 等）。验证链全绿：import OK、`MLX_WHISPER_AVAILABLE=True`、tiny 模型 downloaded=True、API `mlx_whisper_available=True`。后端 kill 后持久服务自动拉起（PID 60626）。教训：国内环境装大包直接上清华镜像；WorkBuddy 会话内长时后台任务会被回收，续做前先确认 task 还在。
- 未把 mlx_whisper 写进 requirements.txt：该文件跨平台共用，Linux/Docker 装不上 macOS-only 的 mlx；如要声明需加平台标记（`mlx_whisper; sys_platform == "darwin"`）。
- 结论：卡片残留是浏览器端 IndexedDB 里 zustand persist 的陈旧状态；3 秒轮询 refreshHistoryList 会以服务端为准整体替换 tasks，但后台标签页会被浏览器节流、且本机网络间歇抽风可能导致轮询失败，卡片就会一直挂着。
- 处理：硬刷新页面即可。另注意 backend/bili_note.db 是 8 月 22 日的陈旧副本（live 库是根目录 bili_note.db），勿混淆。

## 浏览器实测删除流程（18:57）
- 用 agent-browser 在 3015 前端实测：点卡片删除按钮 → 确认弹窗 → 卡片即时消失，DB/history 同步减一（video 7677293929390169595 从 3 条减到 2 条，删的是最新一条 bdbc2e4b）。
- 结论：删除链路（UI → API → DB → 列表刷新）全通。此前用户看到的"删除后还在"确认为浏览器本地 IndexedDB 陈旧状态，刷新即清。
- 该视频剩余 2 条重复失败任务（失败原因：抖音 Cookie 失效），如需可继续清理。

## 排查视频 7650062738210819363 失败原因（19:10）
- 任务 4c1e912d：下载✅、转写✅（transcript.json 有中文全文），挂在前端 **LLM 总结阶段**（markdown.gpt.checkpoint phase=summarize）。
- 根因：`Error code:403 request_quota_exhausted 套餐请求次数已用完`，供应商 "infer ai"(bb478bfd, base https://api.inferaiapi.com/v1) model=deepseek-v4-pro，该 key 套餐额度用尽。
- 现状：19:10 连通性测试返回 200105“API 地址不正确”——可能叠加 Mac 间歇 DNS 问题连不上该外部域名。
- 建议：充额/换供应商（ds/openapi/openapi-test 指向 openapi.xyxmh.cn/v1 但 enabled=0 需启用，或填自有 key 的 DeepSeek 等）；转写已缓存，修复后点卡片「重试」只重跑总结。

## 验证新 infer ai key（19:27）
- 用户提供新 key（sk-34ae41...）想替换 infer ai。实测：域名可达（401 2.5s 返回），但该 key 被供应商驳回 `INVALID_API_KEY` → 此 key 不可用。
- 已把供应商 key 还原回原 sk-1b22bbc...（该 key 合法但套餐额度耗尽=403），避免配置留在无效状态。
- 结论：需用户提供一个真正有效的 infer ai key，或换其他有额度的供应商。

## 验证 infer ai 新 key（19:36-19:46）
- 用户提供 sk-07e636... 实测：**有效**！19:36 直连 /v1/chat/completions 返回 200 + 正常对话。已写入 infer ai(bb478bfd) 并 enabled=1。
- 应用内 connect_test 仍报 200105「API 地址不正确」≠ key 问题：app 所有 LLM 请求走本地代理 127.0.0.1:59519(sandbox-c 进程,存活)；19:46 复测走代理/直连 api.inferaiapi.com 均 http=000 超时，但 DNS 正常解析(158.247.239.18) → 当前是该域名 TCP 出网被间歇阻断（Mac 网络抽风），非 key/app 配置错误。
- 现状：key 可用已就位，待本机网络恢复后，前端卡片「重试」即可用此 key 重跑总结（转写已缓存）。

## 网络失败再诊（19:53）修正结论
- 不是整机断网：baidu 直连 200/0.1s，ping 1.1.1.1 通，Wi-Fi active。
- 本地代理 127.0.0.1:59519(sandbox-c) 转发正常：代理→baidu 200/0.4s；但其海外上游异常：代理→api.inferaiapi.com 返回 502(12.6s)，→api.openai.com 也超时。
- 即：BiliNote/key 配置全部无误，当前是「访问 api.inferaiapi.com 的海外出口」临时故障(502)。上一轮 19:36 直连曾 200 是因那时出口通畅。
- 处置：等海外出口恢复或重连代理/VPN 后，前端卡片「重试」即可（转写已缓存）。应用报错"检查网络连接或稍后再试"准确指向网络，非 app 故障。

## ffmpeg 不可用修复（22:20）
- 现象：`/api/sys_health` 报 `"ffmpeg":"missing"`。
- 根因：启动脚本里 `FFMPEG_BIN_PATH=/Users/jiuyue/.local/bin` 是旧路径，该目录已无 ffmpeg；ffmpeg 实际由 mise 管理，位于 `~/.local/share/mise/installs/ffmpeg/9.0.1/.mise-bins/`。后端 `ffmpeg_command.py` 用 FFMPEG_BIN_PATH 拼接可执行路径，指向不存在文件 → 判定 missing。
- 修复：`启动BiliNote.command` 的 `FFMPEG_BIN_DIR` 改为 `mise which ffmpeg | xargs dirname` 动态解析 + mise installs 目录扫描兜底，不再写死路径；后端已用新路径重启，sys_health 恢复 `"ffmpeg":"ok"`。
- 注意：mise 路径含版本号（9.0.1），升级后会变，任何脚本都不要硬编码。
