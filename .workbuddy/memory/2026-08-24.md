- 启动 BiliNote 时发现 `启动BiliNote.command` 指向不存在的 `/.venv/bin/python`，实际后端环境为 `backend/.venv313/bin/python`。
- 已手动启动前端（3015）和后端（8483），并修复启动脚本：优先使用根目录 `.venv`，否则回退 `backend/.venv313`，最后再回退系统 `python3`。
- 已进一步增强 `启动BiliNote.command`：增加 `lsof/curl/pnpm/open` 依赖检查、前后端端口复用判断、后端 `/api/sys_check` 就绪等待、前端首页就绪等待，以及失败时自动打印最近日志。
- 已验证当前任务状态：调用 `/api/history?include_pending=true` 检查最近 198 条任务，全部为 `SUCCESS`，当前无进行中任务、无暂停任务。
- 已修复批次 `ea63aae0-764d-4550-8560-881fa30b49ba` 完成后前端仍显示“进行中”的问题：前端现在会在后端 `control_state` 为空且 `pending=0` 时自动推导为 `COMPLETED`，批次卡片显示“已完成”，并禁用无意义的暂停/取消按钮；相关测试与前端构建已通过。
- 已定位抖音 `7660398439615712546` 生成失败根因：并非抖音网络错误，而是本地 `fast-whisper tiny` 模型缓存缺失，`WhisperTranscriber` 在任务入队阶段被过早初始化，导致请求长时间阻塞后前端超时报“请求失败，请检查网络连接”。已把 `NoteGenerator` 的转写器初始化改为懒加载，并重启后端加载修复；后续同类失败会进入任务失败态而不是卡在假 `PENDING`。
- 已绕开会导致子进程被挂断的启动方式，改为分别后台启动后端和前端；验证 `8483` 与 `3015` 端口均正常监听，`/api/sys_check` 返回成功，前端首页返回 `200`，并在 10 秒后复查仍持续存活。
- 已确认视频 `7660398439615712546` 的残留任务 `1189b087-b72f-4149-ac8d-8196ec3840c6` 当前只有数据库行，没有结果文件也没有状态文件；后端历史/任务状态接口会把“仅有任务行”的情况强制返回 `PENDING`，因此页面显示“一直等待中”，但实际上没有正在执行的后台任务。
- 已修复后端残留任务判定逻辑：超过短暂宽限期、且既无状态文件也无结果文件、也不在执行中的任务不再返回假 `PENDING`，而是返回“任务未成功启动，请重试”；同时已删除旧残留任务并重提同一抖音链接，新任务 `b0d40d0c-c678-4787-b244-0ef8da110cee` 现已进入真实 `TRANSCRIBING` 状态。
- 已进一步修复本地 `whisper-tiny` 下载问题：确认官方 `huggingface.co` 连接超时而 `hf-mirror.com` 可达，因此为本地启动默认注入 `HF_ENDPOINT=https://hf-mirror.com`，并通过镜像成功下载 `Systran/faster-whisper-tiny`（约 75.5MB `model.bin` 已落盘）。下载完成后再次强制重试，当前新任务 `c47a461d-7b4c-4ad4-ae28-8d5e71bad19f` 正在真实 `TRANSCRIBING`，不再因模型缺失直接失败。
- 已确认同一视频显示“两条任务”的直接原因是前端本地 store 在刷新历史时会保留后端已不再返回的旧任务，形成幽灵卡片；现已修改 `refreshHistoryList` 以服务端历史列表为准，不再把缺席项继续塞回页面。后端当前实际只有一条可见成功任务 `c47a461d-7b4c-4ad4-ae28-8d5e71bad19f` 和一条失败任务 `b0d40d0c-c678-4787-b244-0ef8da110cee`，历史接口只返回成功任务。
- 已定位浏览器插件处理抖音 `7677293929390169595` 失败的真实原因：yt-dlp 明确要求“Fresh cookies”，即当前抖音 Cookie 已失效。此前状态文件之所以只写出“任务失败，未写入详细原因”，是因为 `BizException` 没有把 message 正确暴露给 `str(exc)`。现已修复 `BizException`，重试后新任务 `9cdd078a-8651-4c88-9d6f-b95e0e236448` 的状态文件和接口都能正确返回“抖音 Cookie 已失效，请在设置中更新最新浏览器 Cookie 后重试”。
- 已清理视频 `7677293929390169595` 的两条失败任务 `88d22996-56cb-424d-b09e-95c3815923d9` 与 `9cdd078a-8651-4c88-9d6f-b95e0e236448`；数据库中该视频当前已无残留任务记录。前端若仍显示旧失败项，刷新后应被移除。
- 已调整批次任务面板交互：批次卡片默认保持折叠，不再因聚焦自动展开；状态概览（总数/成功/失败/暂停/取消/进行中及进度条）随批次展开而显示，折叠时隐藏，从而减少列表占用。前端构建已通过。
- 已修复插件重复点击同一抖音链接时的错误拦截：失败且无结果的旧任务不再触发“该视频以前已生成过笔记，请确认是否继续生成”。后端测试 `tests/test_duplicate_note_guard.py` 已通过；实测对 `7677293929390169595` 先失败一次后再次点击，可直接创建新任务 `27faaa0e-8930-49b8-ad75-dff40a82e55b`，不再返回 409 重复确认。
- 已进一步排查“更新 Cookie 后仍失败”的原因：最初误看了 `backend/config/downloader.json`，但当前后端进程实际工作目录在项目根目录，因此读写的是 `config/downloader.json`。在用户点击“从浏览器同步”后，根目录配置文件已更新（mtime 变为 2026-08-24 22:58:28，Cookie 哈希发生变化），说明同步请求确实到达后端；但新值依旧包含异常空键片段（开头出现 `=douyin.com; =douyin.com; ...`），且缺少 `msToken`，用这份最新 Cookie 再次实测仍会立刻报 `Fresh cookies`。为容错，已修复抖音 cookiefile 生成逻辑，忽略空键和 `Domain/Path/Expires/...` 等属性片段；但当前同步进来的 Cookie 本身仍不是 yt-dlp 可接受的新鲜登录态。
- 已开始修复插件抖音 Cookie 同步范围：将插件读取抖音 Cookie 的方式从按 `.douyin.com` 域名枚举改为优先按 `https://www.douyin.com/` URL 可见范围读取，并按 cookie 名去重，目的是覆盖 host-only 与子域 Cookie。随后已为 `BillNote_extension` 安装依赖并成功完成插件构建。
- 用户重新同步后，根目录 `config/downloader.json` 已更新（mtime=2026-08-24 23:26:23，hash 变为 `b30897ea83c5b8d6`），说明同步请求生效；但新值依旧带有空键前缀且缺少 `msToken`，再次重试仍报 Cookie 失效。由此判断：用户浏览器当前实际运行的插件仍是旧代码，尚未重载到新构建版本；需要用户在扩展管理页重新加载/刷新插件后再同步一次，才能验证新的按 URL 取 Cookie 逻辑是否生效。
- 对于“任务等待中”的后续排查：视频 `7677293929390169595` 当前数据库里有多条最新任务记录，但全部是 `FAILED`，最近一条为 `68be0ee7-b9ad-45bb-b21f-ebfc8e353612`；接口 `/api/history?include_pending=true` 当前没有任何 `PENDING/TRANSCRIBING/...` 活跃任务，因此不存在前序任务占用执行队列，这类“等待中”仍更可能是前端本地状态未刷新而非后端真的在排队。
- 用户再次同步后，根目录 `config/downloader.json` 的 mtime 已进一步更新到 2026-08-24 23:48:56，哈希变为 `d759f516aa581110`，说明同步动作确实再次发生；但新值仍保留空键前缀且缺少 `msToken`，与修复后的插件预期不符。进一步判断：浏览器里实际重载的扩展仍可能不是新构建产物，或当前同步动作来自未更新的旧扩展实例。
- 针对浏览器控制台里的 `Unchecked runtime.lastError: The page keeping the extension port is moved into back/forward cache...`，已将内容脚本到后台的通信从 `webext-bridge` 端口消息改为直接 `browser.runtime.sendMessage`，并在背景页添加原生 `runtime.onMessage` 监听，避免页面进入 bfcache 时端口断开触发噪音错误。插件已重新构建通过。
- 针对插件下载配置页报错 `TypeError: Cannot read properties of undefined (reading 'cookie')`，已修复 `Downloader.vue` 首次渲染时 `rows[p]` 尚未初始化的问题：现在在 setup 阶段先为各平台预建行数据，再执行异步刷新，避免模板里 `v-model="rows[p].cookie"` 访问空对象。插件已重新构建通过。
- 为排除 yt-dlp 抖音提取兼容性问题，已将后端虚拟环境中的 yt-dlp 从 `2025.03.31` 升级到 `2026.08.19`，并同步更新 `backend/requirements.txt`。升级后再次重试 `7677293929390169595`，仍然立即报“抖音 Cookie 已失效”，说明当前阻塞点仍是同步出来的 Cookie 内容本身，而不是 yt-dlp 版本过旧。
- 用户贴出的抖音浏览器请求头证明浏览器登录态本身有效（含 `sessionid/sid_tt/ttwid/s_v_web_id/...`），且 `msToken` 出现在查询参数而非 Cookie 中；因此“同步后缺少 msToken Cookie”不是充分判据。当前更可能的问题是服务端 `yt-dlp` 无法完整复现浏览器请求上下文。另已确认下载配置页空指针修复已经进入构建产物：`extension/dist/options/index.html` 引用的 `options-mjYwyLYy.js` 构建时间为 2026-08-25 00:06:48，源码 `Downloader.vue` 已在 setup 阶段预初始化 `rows[p]`，若浏览器仍报 `rows[p].cookie` 空指针，说明运行中的扩展实例仍非最新构建版本，需重新加载指向 `BillNote_extension/extension/` 的本地扩展。
